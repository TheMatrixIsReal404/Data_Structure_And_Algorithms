#include <print>
using namespace std;

int main() {
    // The compiler implicitly welds these isolated lines into one continuous string array
    print(
        "The rose is red,\n"
        "the violet's blue,\n"
        "Sugar is sweet,\n"
        "And so are you.\n"
    );
}